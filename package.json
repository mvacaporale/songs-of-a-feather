{
  "name": "songs-of-a-feather",
  "version": "1.0.0",
  "private": true,
  "description": "Spotify social application with Flask API and Next.js frontend",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:web": "npm run dev --workspace=apps/web",
    "dev:api": "source .venv/bin/activate && cd apps/api && python app.py",
    "build:web": "npm run build --workspace=apps/web",
    "lint:web": "npm run lint --workspace=apps/web",
    "test:api": "source .venv/bin/activate && cd apps/api && pytest",
    "install:all": "npm install && uv venv .venv && source .venv/bin/activate && uv pip install -r apps/api/requirements.txt"
  },
  "devDependencies": {
    "@types/node": "^20"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}